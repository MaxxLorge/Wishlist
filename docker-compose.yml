volumes:
  psql:
  pgadmin:

secrets:
  wishlist_app_secrets:
    file: secret.json

services:
  psql:
    image: postgres
    restart: always
    volumes:
      - psql:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      TZ: Asia/Yekaterinburg
      POSTGRES_PASSWORD: m35pn92x

  wishlist_app:
    image: maxxlorge/wishlist:dev
    restart: always
    ports:
      - 10001:80
    environment:
      TZ: Asia/Yekaterinburg
      ASPNETCORE_URLS: http://localhost:5000
    depends_on:
      - psql
    secrets:
      - source: wishlist_app_secrets
        target: /app/secret.json
